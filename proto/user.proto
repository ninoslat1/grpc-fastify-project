syntax = "proto3";

package user;

service User {
    rpc Login(LoginRequest) returns (LoginPayload);
}

message LoginRequest {
    string user_name = 1;
    string password = 2;
}

message LoginPayload {
    string message = 1;
    int32 status = 2;
}